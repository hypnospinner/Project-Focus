namespace ProjectFocus.Interface
{
    /// <summary>
    /// Each key in this enum + 'Page' produces
    /// a valid page name. This adds protection
    /// from typos in page names when navigating.
    /// </summary>
    public enum PageKey
    {
        Main,
        Problem
    }
}
